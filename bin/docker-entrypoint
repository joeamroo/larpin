#!/bin/bash -e

# If running the rails server then prepare the database and run idempotent seeds
if [ "${@: -2:1}" == "./bin/rails" ] && [ "${@: -1:1}" == "server" ]; then
  ./bin/rails db:prepare db:seed
fi

exec "${@}"
